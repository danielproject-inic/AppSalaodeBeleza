import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.salonsuitepro.collaborator',
  appName: 'Salon Suite Colaborador',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
